# Context

Shared context for the **{{projectName}}** project. All Product OS commands (`/create`, `/dev`, `/verify`, `/release`) read from this folder.

| File | Purpose |
|------|---------|
| **PROJECT_CONTEXT.md** | Product, users, goals, constraints, common non-goals. Used to pre-fill PRDs and skip recurring project-level questions. |
| **TECH_CONTEXT.md** | Technical stack, conventions, and standards. Used for implementation and verification. |
